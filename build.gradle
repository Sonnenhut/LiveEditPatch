buildscript {
    repositories {
        mavenCentral()
    }
}

plugins {
    id "org.jetbrains.intellij" version "0.3.0"
}

apply plugin: 'idea'
apply plugin: 'org.jetbrains.intellij'
apply plugin: 'java'

intellij {
    version '15.0.1'
    intellij.updateSinceUntilBuild false
}

group 'sonnenhut.liveedit.patch'
version '1.0' // Plugin version

publishPlugin {
    username intellijPublishUsername
    password intellijPublishPassword
}

// # commands:
// # create the .zip to upload manually
// gradle assemble
// # open IDE with plugin
// gradle
// gradle publishPlugin